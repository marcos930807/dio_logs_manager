name: dio_logs_manager
description: HTTP Inspector tool for Dart which can debugging http requests for Dio
version: 0.4.1
homepage: https://github.com/marcos930807/dio_logs_manager

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  dio: ^5.7.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
